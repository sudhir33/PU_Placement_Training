#include <stdio.h>
int main() {
    int x = 10;
    if (x > 5)
        printf("x is greater than 5\n");
    else if (x > 2)
        printf("x is greater than 2\n");
    else
        printf("x is not greater than 2\n");
    return 0;
}
